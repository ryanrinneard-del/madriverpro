# Temporarily excluded to keep this deployment under Vercel Hobby's
# 12 Serverless Function limit (api/ currently has 13 functions).
# api/process-arc.js has no callers anywhere in this repo — no frontend
# page invokes it and no other api/ function imports it.
# Re-include this file after upgrading to Vercel Pro or merging an endpoint.
# Added 2026-05-15.
api/process-arc.js

# Dead code as of 2026-05-29: the email-the-Game-Plan logic was MERGED into
# api/build-game-plan-pdf.js (call it with { email: true }). This file is no
# longer referenced by anything and is excluded so it doesn't count toward the
# 12-function Hobby cap. Safe to git-delete the file entirely whenever; until
# then this keeps the deploy at 12 functions.
api/email-game-plan.js
