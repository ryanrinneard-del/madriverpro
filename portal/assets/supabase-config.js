/* =========================================================
   Supabase config — RR Golf Performance (Adult + Junior share)
   Paste the values from Supabase Dashboard → Project Settings
   → API. These are public (the anon key is designed to be
   exposed) — Row Level Security in the database protects data.
   ========================================================= */
window.SUPABASE_CONFIG = {
  url: 'REPLACE_ME_WITH_YOUR_PROJECT_URL',     // https://xxxxxxxxxxxx.supabase.co
  anonKey: 'REPLACE_ME_WITH_YOUR_ANON_KEY',    // eyJhbGciOi... (long JWT)
};
